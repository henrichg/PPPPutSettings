ext.ppVersions = [
    publishVersion: '1.0.6', // this must be >= as in PPP in PPApplication. VERSION_NAME_PPPPS_LATEST
    publishVersionCode: 55,  // this must be >= as in PPP in PPApplication. VERSION_CODE_PPPPS_LATEST

    minSdk: 22,

    targetSdk: 22,
    // 22 is required for:
    // - put settings parameter to system db without root ;-)

    compileSdk: 33,
    buildTools: '34.0.0',

    appcompatXLib: '1.6.1', //'1.5.1',

    autoservice       : '1.1.1',
    acra              : '5.11.2'//'5.10.1'
]
