ext.ppVersions = [
    publishVersion: '1.0.4', // this must be >= as in PPP in PPApplication. VERSION_NAME_PPPPS_LATEST
    publishVersionCode: 45,  // this must be >= as in PPP in PPApplication. VERSION_CODE_PPPPS_LATEST

    minSdk: 22,

    targetSdk: 22,
    // 22 is required for:
    // - put settings parameter to system db without root ;-)

    compileSdk: 33,
    buildTools: '33.0.0',

    appcompatXLib: '1.6.1', //'1.5.1',

    acra: '5.9.8-beta02' //'5.9.7' in this beta is withColor() added in notification configuration ;-)
]
